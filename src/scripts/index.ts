// import TelegramBot from "node-telegram-bot-api";

const runScripts = async (strapi) => {
    // let token = strapi.config.get("server.telegramBotToken");
    // let chatId = strapi.config.get("server.telegramChatId");
    // const bot = new TelegramBot(token, { polling: true });
    // bot.sendMessage(
    //     chatId,
    //     "https://razbor-auto.by/spare-parts/toyota/pol-bagazhnika-5kk2lg-61368"
    // );
};

export default runScripts;
